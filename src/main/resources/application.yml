spring:
  application:
    name: chengq
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://10.100.21.7:3306/chengq?autoReconnect=true&useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: zhiling@123
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: chengq
    password: cheng1993

  redis:
    password: cheng1993 # Redis服务器连接密码（默认为空）
    database: 3 # Redis数据库索引（默认为0）
    port: 6379 # Redis服务器连接端口
    host: localhost # Redis服务器地址


server:
  port: 6000

mybatis-plus:
  global-config:
    enableSqlRunner: true
  configuration:
    # 3.0.8之前版本问题默认将枚举注册为EnumOrdinalTypeHandler,这是错误的方式,默认是 org.apache.ibatis.type.EnumTypeHandler
    # 如果项目之中实体统一使用IEnum或注解的方式,可配置成 com.baomidou.mybatisplus.extension.handlers.EnumTypeHandler,也可省略上面的type-enums-package配置
    # 配置type-enums-package只对注解方式的枚举处理能提前加载缓存.
    default-enum-type-handler: com.baomidou.mybatisplus.extension.handlers.MybatisEnumTypeHandler
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   # 自带sql日志
  mapper-locations: classpath:com/chengq/chengq/mapper/mapping/*Mapper.xml


#mybatis-plus:
#  # 如果是放在src/main/java目录下 classpath:/com/yourpackage/*/mapper/*Mapper.xml
#  # 如果是放在resource目录 classpath:/mapper/*Mapper.xml
#  mapper-locations: classpath:/com/chengq/chengq/mapper/mapping/*.xml
#  #实体扫描，多个package用逗号或者分号分隔
#  typeAliasesPackage: com.chengq.chengq.entity
#  global-config:
#    #主键类型  0:"数据库ID自增", 1:"用户输入ID",2:"全局唯一ID (数字类型唯一ID)", 3:"全局唯一ID UUID";
#    id-type: 0
#    #字段策略 0:"忽略判断",1:"非 NULL 判断"),2:"非空判断"
#    field-strategy: 1
#    #驼峰下划线转换
#    #db-column-underline: true
#    #刷新mapper 调试神器
#    #refresh-mapper: true
#    #数据库大写下划线转换
#    #capital-mode: true
#    # Sequence序列接口实现类配置
#    #key-generator: com.baomidou.mybatisplus.incrementer.OracleKeyGenerator
#    #逻辑删除配置（下面3个配置）
#    #logic-delete-value: 1
#    #logic-not-delete-value: 0
#    #sql-injector: com.baomidou.mybatisplus.mapper.LogicSqlInjector
#    #自定义填充策略接口实现
#    #meta-object-handler: com.baomidou.springboot.MyMetaObjectHandler
#  configuration:
#    map-underscore-to-camel-case: true
#    cache-enabled: false
#    #配置JdbcTypeForNull
#    jdbc-type-for-null: 'null'

logging:
  #配置日志路径地址
  config: classpath:logback-spring.xml

swagger:
  enabled: true

Scheduled:
  enabled: true